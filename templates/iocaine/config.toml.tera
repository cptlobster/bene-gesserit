[server]
bind = "{{ config.binds.iocaine }}"

[sources]
words = "{{ words }}"
markov = [{% for file in corpus %}
    "{{ file }}" {%- if not loop.last -%} , {% endif %} {%- endfor %}
]

{% if config.metrics.enabled -%}
[metrics]
enable = true
bind = "{{ config.binds.metrics.iocaine }}"
{% endif %}